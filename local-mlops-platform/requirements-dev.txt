# Development + CI dependencies (installs runtime deps too).
-r requirements.txt
pytest>=8.0,<9
pytest-cov>=5.0
ruff>=0.6
requests>=2.31  # used by endpoint/integration test helpers
kfp>=2.7,<3  # compile the Kubeflow pipeline in CI
