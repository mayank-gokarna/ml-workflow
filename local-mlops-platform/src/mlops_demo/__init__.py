"""mlops_demo: a small, local, production-style MLOps demo package.

Trains an Iris classifier, tracks experiments with MLflow, and serves the
model through KServe. Kept intentionally small so the whole MLOps lifecycle
runs on a local machine.
"""

__version__ = "0.1.0"
